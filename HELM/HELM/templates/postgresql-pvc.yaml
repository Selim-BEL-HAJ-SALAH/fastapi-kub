apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "my-helm-chart.fullname" . }}-postgresql-pvc
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.postgresql.storage.size }}
